import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'e-vent-mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
